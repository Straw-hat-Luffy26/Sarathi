{
  "name": "sarathi",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "dev:auto": "node scripts/select-backend.mjs dev --release",
    "build:auto": "node scripts/select-backend.mjs build",
    "start": "node scripts/select-backend.mjs dev --release",
    "dev:debug-cpu": "tauri dev",
    "dev:cuda": "tauri dev --release --features cuda",
    "dev:vulkan": "tauri dev --release --features vulkan",
    "build:cuda": "tauri build --features cuda",
    "build:vulkan": "tauri build --features vulkan"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-log": "^2.9.0",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-sql": "^2.4.0",
    "lucide-react": "^1.28.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.18.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
